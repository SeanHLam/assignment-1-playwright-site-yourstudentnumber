
import MenuPopup from "./menuPopup"

export default function NavBar({
  
}) {
 
  return (
    <div class="navCont">
        <MenuPopup></MenuPopup>
        <img class="logo" src="/graduation-hat.png"></img>
    </div>
  )
}
